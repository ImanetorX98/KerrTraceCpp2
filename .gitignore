# Build outputs
build/
build*/
build-ci/
build-cuda/
*.tmp

# Render output: PNG/PPM frames and the .kgeo geodesic buffers, which are large
# (11 GB here) and fully regenerable from the tracer.
out/

# macOS
.DS_Store
.AppleDouble

# Node
server/node_modules/
frontend/node_modules/
frontend/dist/
frontend/.angular/

# C++ compiled
*.o
*.a
*.dylib
*.so

# Editor
.vscode/
.idea/
*.swp
.worktrees/

# Handoff locale per analisi esterna: non versionato di proposito, contiene
# valutazioni di processo che non appartengono alla storia del repo.
HANDOFF-ASTRA.md
